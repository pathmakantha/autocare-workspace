diff --git a/node_modules/jwks-rsa/src/integrations/passport.js b/node_modules/jwks-rsa/src/integrations/passport.js
index 79d75da..618485f 100644
--- a/node_modules/jwks-rsa/src/integrations/passport.js
+++ b/node_modules/jwks-rsa/src/integrations/passport.js
@@ -1,4 +1,3 @@
-const jose = require('jose');
 const { ArgumentError } = require('../errors');
 const { JwksClient } = require('../JwksClient');
 const supportedAlg = require('./config');
@@ -28,25 +27,32 @@ module.exports.passportJwtSecret = function (options) {
   const onError = options.handleSigningKeyError || handleSigningKeyError;
 
   return function secretProvider(req, rawJwtToken, cb) {
-    let decoded;
-    try {
-      decoded = {
-        payload: jose.decodeJwt(rawJwtToken),
-        header: jose.decodeProtectedHeader(rawJwtToken)
-      };
-    } catch (err) {
-      decoded = null;
-    }
-
-    if (!decoded || !supportedAlg.includes(decoded.header.alg)) {
-      return cb(null, null);
-    }
-
-    client.getSigningKey(decoded.header.kid)
-      .then(key => {
-        cb(null, key.publicKey || key.rsaPublicKey);
-      }).catch(err => {
-        onError(err, (newError) => cb(newError, null));
-      });
+    // jose is ESM-only; a top-level require() crashes under bundled/serverless
+    // CJS runtimes (e.g. Vercel) with ERR_REQUIRE_ESM as soon as anything
+    // requires the jwks-rsa package, even if this Passport integration is
+    // never used. Deferring to a dynamic import here avoids that entirely.
+    (async () => {
+      const jose = await import('jose');
+      let decoded;
+      try {
+        decoded = {
+          payload: jose.decodeJwt(rawJwtToken),
+          header: jose.decodeProtectedHeader(rawJwtToken)
+        };
+      } catch (err) {
+        decoded = null;
+      }
+
+      if (!decoded || !supportedAlg.includes(decoded.header.alg)) {
+        return cb(null, null);
+      }
+
+      client.getSigningKey(decoded.header.kid)
+        .then(key => {
+          cb(null, key.publicKey || key.rsaPublicKey);
+        }).catch(err => {
+          onError(err, (newError) => cb(newError, null));
+        });
+    })();
   };
 };
diff --git a/node_modules/jwks-rsa/src/utils.js b/node_modules/jwks-rsa/src/utils.js
index ef3ffb5..32e66f7 100644
--- a/node_modules/jwks-rsa/src/utils.js
+++ b/node_modules/jwks-rsa/src/utils.js
@@ -1,4 +1,3 @@
-const jose = require('jose');
 const JwksError = require('./errors/JwksError');
 
 function resolveAlg(jwk) {
@@ -33,6 +32,10 @@ function resolveAlg(jwk) {
 }
 
 async function retrieveSigningKeys(jwks) {
+  // jose is ESM-only; a top-level require() crashes under bundled/serverless
+  // CJS runtimes (e.g. Vercel) with ERR_REQUIRE_ESM. Dynamic import works
+  // everywhere since it goes through the ESM loader instead of require().
+  const jose = await import('jose');
   const results = [];
 
   jwks = jwks
